apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cilium

sortOptions:
  order: fifo

resources:
  - namespace.yaml
  - gatewayclass.yaml
  - gateway-public.yaml
  - policy.yaml
  - lb-pool.yaml
