---
# Variáveis locais do overlay broetec-storage — editável, não sobrescrito por make inventory.

# Disco maior para dados persistentes (consumido por roles/01_create_vm via hostvars).
vm_disk_size_gb: 120
