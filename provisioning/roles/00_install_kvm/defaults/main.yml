---
# Optional bootstrap (packages + libvirtd). Disable on immutable distros or with --skip-tags bootstrap.
kvm_host_bootstrap: true

# firewalld NAT/forward rules for VMs on the lab libvirt network.
kvm_network_firewalld: true
