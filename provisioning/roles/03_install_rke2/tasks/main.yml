---
# Stub — replace with RKE2 installation (see docs/bootstrap/README.md).

- name: "Alive check before install_rke2 (stub)"
  become: false
  ansible.builtin.ping:

- name: "Placeholder — RKE2 installation pending"
  ansible.builtin.debug:
    msg: >-
      03_install_rke2: implementation pending.
      Host={{ inventory_hostname }}
      overlay={{ overlay_id | default('n/a') }}.
      Guide: docs/bootstrap/README.md
